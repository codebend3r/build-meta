# The CLI itself still ships zero runtime dependencies. Everything in
# devDependencies is toolchain only, so the lockfile is committed and anything
# added by hand is written exact.
[install]
exact = true
